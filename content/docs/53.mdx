---
title: "Maximum Subarray"
difficulty: "Medium"
category: "Array
           Divide and Conquer
           Dynamic Programming"
date: "2025-02-26"
---

# Problem Statement
Given an integer array nums, find the subarray with the largest sum, and return its sum.

Example 1:
```plaintext
Input: nums = [-2,1,-3,4,-1,2,1,-5,4]
Output: 6
Explanation: The subarray [4,-1,2,1] has the largest sum 6.
```

Example 2:
```plaintext
Input: nums = [1]
Output: 1
Explanation: The subarray [1] has the largest sum 1.
```

Example 3:
```plaintext
Input: nums = [5,4,-1,7,8]
Output: 23
Explanation: The subarray [5,4,-1,7,8] has the largest sum 23.
```

# My Solution
```python
def maxSubArray(self, nums: List[int]) -> int:
    max_sum = [num for num in nums]
    max_inc = [num for num in nums]
    for i in range(1, len(nums)):
        max_inc[i] = max(max_inc[i - 1] + nums[i], nums[i])
        max_sum[i] = max(max_inc[i], max_sum[i - 1])
    return max_sum[-1]
```

Explanation
* One array keeps track of maximum subarray including the current number
* One array keeps track of maximum subarray so far (whether or not current number is included)

# Best Solution
```python
def maxSubArray(nums: list[int]) -> int:
    current_sum = max_sum = nums[0]

    for i in range(1, len(nums)):
        current_sum = max(nums[i], current_sum + nums[i])
        max_sum = max(max_sum, current_sum)

    return max_sum
```

Why is this better?
* Kadane’s Algorithm is the best approach to solve this problem efficiently. It works by maintaining a running sum (current_sum) and updating a maximum sum (max_sum).

Complexity Analysis
* Time Complexity: O(n)
* Space Complexity: O(1) for Kadane's (Optimal), O(n) for my solution